import {CONFIG} from "../config/config.js";import {isConfigured} from "./state.js";import {storage} from "./storage.js";
const mockKey="mock-rooms";const read=()=>storage.get(mockKey,{});const write=v=>storage.set(mockKey,v);
async function fetchApi(action,payload={},retries=1){if(!isConfigured())return mock(action,payload);const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),10000);try{const response=await fetch(CONFIG.API_URL,{method:"POST",redirect:"follow",headers:{"Content-Type":"text/plain;charset=utf-8"},body:JSON.stringify({action,...payload}),signal:controller.signal});const body=await response.json();if(!body.success)throw Object.assign(new Error(body.message),{code:body.errorCode});return body.data}catch(error){console.error("API",action,error);if(retries>0&&error.name!=="AbortError")return fetchApi(action,payload,retries-1);throw new Error(error.name==="AbortError"?"連線逾時，請再試一次。":error.message||"目前無法連接共同資料庫，請檢查網路或 Apps Script 設定。")}finally{clearTimeout(timer)}}
function mock(action,p){const rooms=read();if(action==="healthCheck")return Promise.resolve({mode:"mock",message:"Mock 模式正常"});if(action==="createRoom"){rooms[p.room.room_id]=p.room;write(rooms);return Promise.resolve(p.room)}if(action==="getRoom"){const r=rooms[p.room_id];if(!r)throw new Error("這個房間不存在，可能連結不完整。");if(r.room_token!==p.room_token)throw new Error("邀請連結驗證失敗。");return Promise.resolve(structure(r,p.user_id))}if(action==="submitAnswer"){const r=rooms[p.room_id];if(!r||r.room_token!==p.room_token)throw new Error("房間不存在或驗證失敗。");if(r.answers[p.user_id])throw new Error("你已經回答過囉。");r.answers[p.user_id]=p.payload;r.updated_at=new Date().toISOString();if(Object.keys(r.answers).length===2){r.status="completed";r.result=p.result||null;r.completed_at=r.updated_at}write(rooms);return Promise.resolve(structure(r,p.user_id))}if(action==="getPendingRooms")return Promise.resolve(Object.values(rooms).filter(r=>r.status==="waiting"&&(r.creator_user_id===p.user_id||r.partner_user_id===p.user_id)).map(r=>structure(r,p.user_id)));if(action==="cancelRoom"){delete rooms[p.room_id];write(rooms);return Promise.resolve({});}throw new Error("Mock 模式不支援此操作");}
function structure(r,user){const safe={...r,answers:{}};if(r.status==="completed")safe.answers=r.answers;else if(r.answers[user])safe.answers[user]=r.answers[user];return safe}
export const api={call:fetchApi,health:()=>fetchApi("healthCheck"),createRoom:room=>fetchApi("createRoom",{room}),getRoom:(room_id,room_token,user_id)=>fetchApi("getRoom",{room_id,room_token,user_id}),submitAnswer:p=>fetchApi("submitAnswer",p),pending:user_id=>fetchApi("getPendingRooms",{user_id}),cancel:p=>fetchApi("cancelRoom",p),history:user_id=>fetchApi("getHistory",{user_id})};
